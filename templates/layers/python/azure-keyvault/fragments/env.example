
# Azure Key Vault — DefaultAzureCredential reused from auth/credential.py
# Required. Find the URL in the Azure Portal: Key Vault > Overview > Vault URI.
# Format: https://<vault-name>.vault.azure.net/
AZURE_KEYVAULT_URL=
