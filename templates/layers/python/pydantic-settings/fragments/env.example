# -- dropwsl:pydantic-settings --
# Typed application settings (parsed by config/settings.py).
# All variables are optional and have sensible defaults; override per environment.
APP_NAME={{PROJECT_NAME}}
APP_ENV=development
LOG_LEVEL=INFO
