# Azure AI Chat — optional default model for chat endpoints
# Set to a deployment name with chat_completion capability.
# Discover available models: GET /api/models
# If not set, pass "model" in the request body of /api/chat.
AZURE_AI_CHAT_MODEL=
