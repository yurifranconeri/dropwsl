## Product

- Requirements and user stories live in `docs/` and GitHub Issues
- Acceptance criteria use Given/When/Then (Gherkin) format
- Specs follow the template: Problem → Context → Requirements → ACs → Out of scope
- Release notes describe user-facing impact, not implementation details
- The PO does not write code — when implementation is needed, delegate to @developer
